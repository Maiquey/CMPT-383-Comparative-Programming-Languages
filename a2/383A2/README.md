Must compile all modules for main drivers to work!

To run exercise re-implementations:

  >> main:start().

To run samples 1 and 3:

  >> sample_main:start().

To run sample 2 (concurrency):

  >> sample_main:concurrencyTest().

(Ctrl+C to exit)